import { AgeFinderPipe } from './age-finder.pipe';

describe('AgeFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
